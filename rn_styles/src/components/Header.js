import React from 'react';
import { Text, View } from 'react-native';

const Header = ({ style }) => {
  return (
    <View style={style}>
      <Text>Header</Text>
    </View>
  );
};

export default Header;
