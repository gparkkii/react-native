import React from 'react';
import { Text, View } from 'react-native';

const Footer = ({ style }) => {
  return (
    <View style={style}>
      <Text>Footer</Text>
    </View>
  );
};

export default Footer;
